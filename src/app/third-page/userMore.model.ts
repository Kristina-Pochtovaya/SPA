export class UserMore {
    male: string;
    children: boolean;
    job: boolean;

    constructor(male: string, children: boolean, job: boolean) {
        this.male = male;
        this.children = children;
        this.job = job;
    }
}